import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.selffinance.app',
  appName: 'ngweb',
  webDir: 'docs',
  bundledWebRuntime: false
};

export default config;
